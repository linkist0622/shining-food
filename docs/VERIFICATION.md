# 検証記録

2026-09-19（日本時間）。通常の注文・決済は実行していません。

## v2.0時点の実施済み（履歴）

- TypeScript型検査、対象コンポーネントのESLint、Vinext本番ビルド。
- 状態遷移テスト: ホテル1130、スウィートグラス、管理センター、範囲外承認、範囲外お断り、BBQ6名。12施設候補の選択・相談必須。支払4方式、失敗・再試行、連打、古い完了の無視、見積の失効、追加質問、代替案、リセット。40候補件数。参考送料1000/2000/10000円以上無料。
- ブラウザー: 390px・320pxの実ページiframe表示。下部5項目、上部固定バー、入力・選択肢、施設F-001〜F-012、商品カテゴリ。スクロールバーを除く実幅373px・303pxでdocument scrollWidthとclientWidthが一致。
- ブラウザー通常注文: ホテル1130、日時・本人受渡し・連絡先→白画面→店舗受付→白画面→PayPay選択→模擬支払→注文完了→情報消去→イントロ→スキップ→空カート。
- ブラウザー追加相談: F-012→店舗から追加質問→顧客回答→店舗再確認→正式見積→顧客側に見積表示。
- 商品群写真: ローカル原本13枚は目視確認（原価・個人情報なし）。公開Notion参照3群はアプリ内でもnaturalWidth>0を確認。個別24商品にも商品群写真を継承、ラベルを明示。
- イントロ: 自動でメニューに移ること、リセット直後のスキップをブラウザーで確認。タイマーは10秒、画像読み込みに依存せず、unmountで全解除。
- ブラウザーで見つかった日付入力の未反映とHTTP環境のcrypto.randomUUID非対応を修正。

## コードで確認済み／実端末未検証

- 画像読み込み遅延時の10秒上限、prefers-reduced-motionで0.7秒、タイマー解除はコード検査。
- safe-area対応、200%文字拡大、横向き、OSキーボードはCSS設計済み。実iPhone/Androidの操作は未検証。
- PWA manifest・PNGアイコン・SW・オフライン専用ページ・更新案内は実装。開発ブラウザーがHTTPのため、インストール・SW更新・オフライン再起動を実端末で未検証。
- 模擬支払失敗・再試行・遅延応答・二重開始の拒否は状態遷移テストで検証。ブラウザーの通信断操作は未実施。
- 全取引データはReactのメモリーのみ。独自保存DBなし。リセットはアプリprefixのsessionStorage/localStorageも削除。本番の注文履歴削除とは非共用。
- Stripe API、通知送信、施設連絡、予約APIはソースに接続がなく、入力を外部へ送る処理なし。商品写真3群の読み込みだけはNotionへの画像GETを伴う。

各6経路すべてのブラウザー操作を実施したという報告ではありません。分岐全体のテストと、ブラウザーの代表経路を組み合わせています。


## v2.1 施設名・最低注文金額の変更（2026-09-19）

- 台帳v0.1 `04_施設実走!A4:B15` とF-001〜F-012の施設名を照合。画面は12施設名で表示し、重複する3つの別操作例を統合。全施設の許可・配達可否は未確認のため見積相談を必須とした。
- ユーザーの追記どおり、最低注文金額3,980円はデリバリーのみ。送料は対象外。テイクアウトは最低注文金額なし。
- 状態遷移テスト：3,979円は拒否、3,980円・3,981円は申込み可能。3,600円＋送料1,000円でも拒否。テイクアウト450円は申込み可能。配達／持帰り切替、BBQ、空カート、値減り後の見積失効を確認。既存6経路と模擬支払のガードも通過。
- TypeScript型検査・Vinext本番ビルドに成功。
- ブラウザー：商品3,600円＋送料1,000円のカートで「あと380円」と申込み停止を確認。数量を増やすと申込みボタンが有効化。長い実名を注文先に選択可能。
- ブラウザー：390px iframeでテイクアウト450円の最低金額なし表示と申込みボタン有効を確認。320px iframeの施設一覧は12件。両iframeの有効幅373px・303pxでscrollWidthとclientWidthが一致。実端末ではない。
- コードとUIから、実注文・決済・通知の接続を追加していないことを確認。

今回の施設名の直接参照はv0.1。最新版v0.4のダウンロードは失敗しており、最新版の内容確認済みとはしていない。
